language: julia
os:
  - linux
before_install:
  - echo $JAVA_HOME
  - ls -l /usr/lib/jvm/*
  - ls -l /urs/local/lib/jvm/*
julia:
  - 1.0

branches:
  only:
    - master
    - /release-.*/

notifications:
  email: false
