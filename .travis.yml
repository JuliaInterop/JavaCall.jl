dist: bionic
language: julia
os:
  - linux
env:
  - JAVA_HOME=/usr/local/lib/jvm/openjdk11
  - JAVA_HOME=/usr/local/lib/jvm/openjdk8
before_install:
  - ls -l /usr/lib/jvm/*
  - ls -l /usr/local/lib/jvm/*
julia:
  - 1.0

branches:
  only:
    - master
    - /release-.*/

notifications:
  email: false
