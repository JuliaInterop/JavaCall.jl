language: julia
os:
  - linux
before_install:
  - echo $JAVA_HOME
  - ls -l /usr/lib/jvm/*
  - ls -l /usr/local/lib/jvm/*
  - export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
julia:
  - 1.0

branches:
  only:
    - master
    - /release-.*/

notifications:
  email: false
